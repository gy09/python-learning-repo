A_list = ['a', 'b', 'c', 'd', 'e', 'f']

for index in range(len(A_list)):
    value = A_list[index]
    print(index, value)

print(A_list)

index = 0
for val in A_list:
    print(index, val)
    index = index + 1

